<nav class="navbar navbar-inverse navbar-static-top">
    <div class="container">
        <div class="navbar-header">

            {# Burger icon #}
            {% if auth %}
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            {% endif %}

            <a href="{{ urlFor('home') }}" class="navbar-brand">Funnel CMS</a>
        </div>

        {# Navigation #}
        {% if auth %}
            <div class="collapse navbar-collapse" id="navbar-collapse">

                {# Left navigation #}
                    <ul class="nav navbar-nav">
                        <li><a href="{{ urlFor('page.all') }}">{{ translator.get('Pages') }}</a></li>
                        <li><a href="{{ urlFor('article.all') }}">{{ translator.get('Articles') }}</a></li>
                        <li><a href="{{ urlFor('newsletter.all') }}">{{ translator.get('Newsletters') }}</a></li>
                        <li><a href="{{ urlFor('file.all') }}">{{ translator.get('Files') }}</a></li>

                        {% if auth.isAdmin() %}
                            <li><a href="{{ urlFor('user.all') }}">{{ translator.get('Users') }}</a></li>
                        {% endif %}
                    </ul>

                {# Right navigation #}
                <ul class="nav navbar-nav navbar-right">
                    {% if auth %}
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                {{ auth.getFullNameOrEmail() }} <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{ urlFor('account.profile') }}">{{ translator.get('ChangeInformation') }}</a></li>
                                <li><a href="{{ urlFor('auth.password.change') }}">{{ translator.get('ChangePassword') }}</a></li>
                            </ul>
                        </li>
                        <li><a href="{{ urlFor('logout') }}">{{ translator.get('Logout') }}</a></li>
                    {% endif %}
                </ul>
            </div>
        {% endif %}
    </div>
</nav>