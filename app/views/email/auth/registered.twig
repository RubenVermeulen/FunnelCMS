{% extends 'email/templates/default.twig' %}

{% block content %}
    <p>Er is een account voor je aangemaakt op Funnel CMS!</p>
    <p>Activeer je account door op anderstaande URL te klikken:</p>
    <a href="{{ baseUrl }}{{ urlFor('user.activate') }}?email={{ user.email }}&identifier={{ identifier|url_encode }}">{{ baseUrl }}{{ urlFor('user.activate') }}?email={{ user.email }}&identifier={{ identifier|url_encode }}</a>
{% endblock %}